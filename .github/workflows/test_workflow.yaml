name: test_workflow

on:
  pull_request:
    paths:
      - 'tests/*'
      - 'requirements.txt'
      - '.github/workflows/test_workflow.yaml'
      - '.github/workflows/demo_workflow.yaml'
      - 'pull_request_test.txt'
  push:
    branches:
      - main

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install python
        uses: actions/setup-python@v4
      - name: Setup cache
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest --cov